<base:WpfUiWindow x:Class="Bloxstrap.UI.Elements.Dialogs.UpdateCompleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:Bloxstrap.UI.Elements.Base"
        xmlns:resources="clr-namespace:Bloxstrap.Resources"
        mc:Ignorable="d"
        Width="500"
        SizeToContent="Height"
        Title="{x:Static resources:Strings.Update_Complete_Title}"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <Grid Background="{DynamicResource MainWindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" ShowMinimize="False" ShowMaximize="False" CanMaximize="False"
                     KeyboardNavigation.TabNavigation="None"
                     Title="{x:Static resources:Strings.Update_Complete_Title}" />

        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Width="36" Height="36" Margin="0,0,16,0" VerticalAlignment="Top"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Source="pack://application:,,,/Resources/MessageBox/Information.png" />
            <StackPanel Grid.Column="1">
                <TextBlock x:Name="TitleTextBlock"
                           Text="{x:Static resources:Strings.Update_Complete_Title}"
                           FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           TextWrapping="Wrap" />
                <TextBlock x:Name="MessageTextBlock" Margin="0,14,0,0"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Padding="18" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}">
            <Button x:Name="CloseButton" MinWidth="100" HorizontalAlignment="Right"
                    Content="{x:Static resources:Strings.Common_Close}" />
        </Border>
    </Grid>
</base:WpfUiWindow>
