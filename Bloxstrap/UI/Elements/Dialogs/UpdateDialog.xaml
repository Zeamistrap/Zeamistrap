<base:WpfUiWindow x:Class="Bloxstrap.UI.Elements.Dialogs.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:Bloxstrap.UI.Elements.Base"
        xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
        xmlns:resources="clr-namespace:Bloxstrap.Resources"
        mc:Ignorable="d"
        Width="680"
        Height="520"
        MinWidth="600"
        MinHeight="460"
        MaxWidth="820"
        MaxHeight="760"
        Title="Zeamistrap"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <Grid Background="{ui:ThemeResource ApplicationBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" ShowMinimize="False" ShowMaximize="False" CanMaximize="False"
                     KeyboardNavigation.TabNavigation="None"
                     Title="Zeamistrap"
                     Icon="pack://application:,,,/Zeamistrap.png" />

        <Grid Grid.Row="1" Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image Grid.Row="0" Grid.Column="0" Width="80" Height="80" Margin="0,0,20,0" VerticalAlignment="Top"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Stretch="Uniform"
                   Source="pack://application:,,,/Zeamistrap.png" />

            <Grid Grid.Row="0" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="NewVersionTextBlock" Grid.Row="0"
                           Text="{x:Static resources:Strings.Update_Available_NewVersion}"
                           FontSize="18" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           TextWrapping="Wrap" />

                <StackPanel Grid.Row="1" Margin="0,18,0,0" Orientation="Horizontal">
                    <TextBlock x:Name="CurrentVersionTextBlock"
                               FontSize="16" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                    <TextBlock Text="→" Margin="12,0" FontSize="16"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock x:Name="LatestVersionTextBlock"
                               FontSize="16" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,20,0,8" Orientation="Horizontal">
                <TextBlock Text="{x:Static resources:Strings.Update_Available_Notes}"
                           FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock Margin="12,0,0,0" VerticalAlignment="Center">
                    <Hyperlink x:Name="ReleaseNotesLink"
                               RequestNavigate="ReleaseNotesLink_RequestNavigate">
                        <Run Text="{x:Static resources:Strings.Update_Available_NotesSource}" />
                    </Hyperlink>
                </TextBlock>
            </StackPanel>

            <Border Grid.Row="2" Grid.ColumnSpan="2" Padding="12"
                    Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}"
                    BorderBrush="{ui:ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <controls:MarkdownTextBlock x:Name="ReleaseNotesTextBlock"
                                                Padding="0,2,8,2"
                                                TextWrapping="Wrap"
                                                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                </ScrollViewer>
            </Border>
        </Grid>

        <Border Grid.Row="2" Padding="18" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}">
            <StackPanel Orientation="Horizontal" FlowDirection="LeftToRight" HorizontalAlignment="Right">
                <Button x:Name="LaterButton" MinWidth="100"
                        Content="{x:Static resources:Strings.Update_Available_Later}" />
                <Button x:Name="UpdateButton" MinWidth="120" Margin="12,0,0,0"
                        Content="{x:Static resources:Strings.Update_Available_Confirm}" />
            </StackPanel>
        </Border>
    </Grid>
</base:WpfUiWindow>
